spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: ipv6.maretu.top:8848
        file-extension: yaml
        refresh-enabled: true
      discovery:
        server-addr: ipv6.maretu.top:8848