spring:
  application:
    name: user-service
  cloud:
    nacos:
      config:
        server-addr: ipv6.maretu.top:8848
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yaml
      discovery:
        server-addr: ipv6.maretu.top:8848