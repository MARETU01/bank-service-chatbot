server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
    nacos:
      server-addr: ipv6.maretu.top:8848
      username: nacos
      password: maretu
  config:
    import:
      - "nacos:gateway-config.yaml?group=DEFAULT_GROUP&refresh=true"

maretu:
  auth:
    excludePaths:
      - /users/**
